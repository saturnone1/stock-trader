# Leave VITE_API_URL empty to use the recommended same-origin /api route.
VITE_API_URL=
# Used only by `npm run dev`; the browser still calls the local Vite origin.
VITE_DEV_API_PROXY_TARGET=http://localhost:5239
