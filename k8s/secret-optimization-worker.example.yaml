apiVersion: v1
kind: Secret
metadata:
  name: stocktrader-optimization-worker-auth
  namespace: stocktrader
type: Opaque
stringData:
  # Generate an independent value with at least 32 characters. Never commit the real value.
  shared-secret: REPLACE_WITH_A_RANDOM_32_PLUS_CHARACTER_SECRET
